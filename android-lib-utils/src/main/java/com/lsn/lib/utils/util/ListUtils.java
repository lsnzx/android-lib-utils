package com.lsn.lib.utils.util;

/**
 * <pre>
 *     author: Blankj
 *     blog  : http://blankj.com
 *     time  : 2019/07/09
 *     desc  : utils about api
 * </pre>
 */
public final class ListUtils {

    private static final String TAG = "ApiUtils";


/*    public static ArrayList<ArrayList<T>> splitList(List<T> list, int num) {
        int listSize = list.size();
        ArrayList<ArrayList<T>> objects1 = new ArrayList();
        int n = 0;

        for (int i = 0; i < listSize / num; i++){

        }

            for (i in 0 until listSize /num){
            val objects:java.util.ArrayList<T> =java.util.ArrayList()
            while (n < num * (i + 1)) {
                objects.add(list[n])
                n++
            }
            objects1.add(objects)
        }
        //处理余数问题
        val a = if (listSize % num != 0) listSize % num
        else 0
        for (i in 0 until a){
            val objects:java.util.ArrayList<T> =java.util.ArrayList()
            while (n < num * (listSize / num) + a) {
                objects.add(list[n])
                n++
            }
            if (!CollectionUtils.isEmpty(objects)) {
                objects1.add(objects)
            }
        }
        return objects1
    }*/


}